#include "main.h"

/**
 * add - function that adds two integers and prints the results
 * @a: first number input
 * @b: second number input
 * Return: a + b
 */
int add(int a, int b)
{
	return (a + b);
}
